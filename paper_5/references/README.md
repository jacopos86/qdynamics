# References

This folder stores external reference inputs for the Paper 5 workspace.

Downloaded PDFs live in `pdfs/`. Source metadata, local file paths, page counts,
and SHA-256 hashes live in `sources.json`.

When refreshing a PDF, update both `sources.json` and `sources.bib`.
