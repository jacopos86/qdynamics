# Open File Path Snapshots

This folder stores user-facing path snapshots for local files that were open in
desktop apps and preserved so the user can safely close windows without losing
track of paths.

The current snapshot is:

- `current_chrome_finder_open_paths.md`

