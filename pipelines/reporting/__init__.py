"""Report/document compilation entrypoints.

These modules are intentionally separated from execution workflows so the
run surface can stay focused on producing artifacts, while this package
focuses on converting existing JSON/artifact bundles into PDFs and summary
documents.
"""
