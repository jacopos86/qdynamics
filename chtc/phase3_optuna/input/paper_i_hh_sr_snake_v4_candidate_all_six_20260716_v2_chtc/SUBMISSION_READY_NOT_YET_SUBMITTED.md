# Submission-ready bundle — not yet submitted

The user authorized CHTC submission and every recorded local, archive-only,
scientific-smoke, source-locked regression, and remote image/backend gate has
passed. The v4 contract and all normalized manifests lock the finite-angle
fallback and Phase-III rescue off; the command parser locks the Phase-III
oracle-gradient mode off. `submit.sub` is enabled with
`requirements = TARGET.HasSIF`.

No `condor_submit` command was run while building this bundle. The authenticated
main agent owns transfer and submission and must preserve the hashes in
`submission_artifact_hashes.json`.
