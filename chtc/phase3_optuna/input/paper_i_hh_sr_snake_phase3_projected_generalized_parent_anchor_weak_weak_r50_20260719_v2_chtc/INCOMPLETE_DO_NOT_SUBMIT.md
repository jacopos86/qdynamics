# Incomplete source-lock revision

This unsubmitted v2 anchor stopped during archive-only import validation. The
preflight showed that the updated CLI also requires the corresponding updated
formal-manifold outer-information module. Preserve this directory as failed
build evidence; use the immutable v3 successor instead.
