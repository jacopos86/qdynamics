# Superseded source-lock revision

This unsubmitted v4 anchor passed archive-only execution validation, but its
anchor plan retained the parent result's temporary extraction path. The
scientific bytes were valid; the source pointer was not durable. Use immutable
v5, which identifies the preserved transfer archive, internal result member,
result SHA-256, and local validation receipt explicitly.
