# Incomplete source-lock revision

This unsubmitted v3 anchor reached archive-only worker validation, which
correctly rejected a stale independently hash-locked outer-information module
receipt. Preserve this directory as failed build evidence; the immutable v4
successor propagates all overlay SHA changes through the worker and manifests.
